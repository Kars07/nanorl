[project]
name = "tiny-long-horizon-v1"
version = "0.1.0"
description = "tiny-long-horizon-v1 — <one-line description>."
requires-python = ">=3.11"
dependencies = ["verifiers"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["tiny_long_horizon_v1"]
