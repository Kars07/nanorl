[project]
name = "tiny-tooluse-v1"
version = "0.1.0"
description = "tiny-tooluse-v1 — <one-line description>."
requires-python = ">=3.11"
dependencies = ["verifiers"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["tiny_tooluse_v1"]
